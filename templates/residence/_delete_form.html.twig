<form class="m-3" method="post" action="{{ path('app_residence_delete', {'id': residence.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ residence.id) }}">
    <button class="btn btn-danger">Supprimer résidence</button>
</form>
