{% extends 'base.html.twig' %}

{% block title %}Gestionnaire{% endblock %}

{% block body %}
    <h1>Gestionnaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ gestionnaire.id }}</td>
            </tr>
            <tr>
                <th>NomGestionnaire</th>
                <td>{{ gestionnaire.nomGestionnaire }}</td>
            </tr>
            <tr>
                <th>AdresseGestionnaire</th>
                <td>{{ gestionnaire.adresseGestionnaire }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_gestionnaire_index') }}">back to list</a>

    <a href="{{ path('app_gestionnaire_edit', {'id': gestionnaire.id}) }}">edit</a>

    {{ include('gestionnaire/_delete_form.html.twig') }}
{% endblock %}
