{% extends 'base.html.twig' %}

{% block title %}Edit Gestionnaire{% endblock %}

{% block body %}
    <h1>Modifier Gestionnaire {{ gestionnaire.nomGestionnaire }}</h1>

    {{ include('gestionnaire/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <div>
        <button class="mt-3 btn btn-secondary">
            <a href="{{ path('app_gestionnaire_index') }}">Retour liste gestionnaires</a>
        </button>
    </div>
    <div>
        <button class="mt-3 btn btn-secondary">
            <a href="{{ path('app_residence_index') }}">Retour liste résidences</a>
        </button>
    </div>

    {{ include('gestionnaire/_delete_form.html.twig') }}
{% endblock %}
