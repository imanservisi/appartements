<form method="post" action="{{ path('app_taxe_fonciere_delete', {'residenceId': residence.id, 'id': taxe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ taxe.id) }}">
    <button class="mt-3 btn btn-danger">Supprimer</button>
</form>
