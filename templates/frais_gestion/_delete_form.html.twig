<form method="post" action="{{ path('app_frais_gestion_delete', {'residenceId': residence.id, 'lotId': lot.id, 'mandatGestionnaireId': mandat_gestionnaire.id, 'id': frais_gestion.id}) }}" onsubmit="return confirm('Etes vous sûr de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ frais_gestion.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
