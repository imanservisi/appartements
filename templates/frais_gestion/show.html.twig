{% extends 'base.html.twig' %}

{% block title %}FraisGestion{% endblock %}

{% block body %}
    <h1>FraisGestion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ frais_gestion.id }}</td>
            </tr>
            <tr>
                <th>Annee</th>
                <td>{{ frais_gestion.annee }}</td>
            </tr>
            <tr>
                <th>Mois</th>
                <td>{{ frais_gestion.mois }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ frais_gestion.montant }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_frais_gestion_index') }}">back to list</a>

    <a href="{{ path('app_frais_gestion_edit', {'id': frais_gestion.id}) }}">edit</a>

    {{ include('frais_gestion/_delete_form.html.twig') }}
{% endblock %}
