{% extends 'base.html.twig' %}

{% block title %}Caf{% endblock %}

{% block body %}
    <h1>Caf</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ caf.id }}</td>
            </tr>
            <tr>
                <th>Annee</th>
                <td>{{ caf.annee }}</td>
            </tr>
            <tr>
                <th>Mois</th>
                <td>{{ caf.mois }}</td>
            </tr>
            <tr>
                <th>MontantCaf</th>
                <td>{{ caf.montantCaf }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_caf_index') }}">back to list</a>

    <a href="{{ path('app_caf_edit', {'id': caf.id}) }}">edit</a>

    {{ include('caf/_delete_form.html.twig') }}
{% endblock %}
