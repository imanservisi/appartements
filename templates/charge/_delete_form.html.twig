<form method="post" action="{{ path('app_charge_delete', {'residenceId': residence.id, 'lotId': lot.id, 'id': charge.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ charge.id) }}">
    <button class="btn"><i class="fa-solid fa-trash"></i></button>
</form>
