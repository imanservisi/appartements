{% extends 'base.html.twig' %}

{% block title %}Emprunt{% endblock %}

{% block body %}
    <h1 class="text-center">Modifier Emprunt</h1>

    {{ include('emprunt/_form.html.twig', {'button_label': 'Mise à jour'}) }}

    <div class="mt-3 card">
        {{ include('interet/index.html.twig', {empruntId: emprunt.id}) }}
    </div>

    <button class="mt-3 btn btn-secondary" type="button">
        <a href="{{ path('app_lot_edit', {residenceId: residence.id, id: lot.id }) }}">Retour {{ lot.nomLot }}</a>
    </button>
{% endblock %}
