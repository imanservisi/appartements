{% extends 'base.html.twig' %}

{% block title %}Emprunt{% endblock %}

{% block body %}
    <h1>Emprunt</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ emprunt.id }}</td>
            </tr>
            <tr>
                <th>DebutEmprunt</th>
                <td>{{ emprunt.debutEmprunt ? emprunt.debutEmprunt|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>AnneeEmprunt</th>
                <td>{{ emprunt.anneeEmprunt }}</td>
            </tr>
            <tr>
                <th>MontantEmprunt</th>
                <td>{{ emprunt.montantEmprunt }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_emprunt_index') }}">back to list</a>

    <a href="{{ path('app_emprunt_edit', {'id': emprunt.id}) }}">edit</a>

    {{ include('emprunt/_delete_form.html.twig') }}
{% endblock %}
