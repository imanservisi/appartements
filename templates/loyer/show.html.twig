{% extends 'base.html.twig' %}

{% block title %}Loyer{% endblock %}

{% block body %}
    <h1>Loyer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ loyer.id }}</td>
            </tr>
            <tr>
                <th>Annee</th>
                <td>{{ loyer.annee }}</td>
            </tr>
            <tr>
                <th>Mois</th>
                <td>{{ loyer.mois }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ loyer.montant }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_loyer_index') }}">back to list</a>

    <a href="{{ path('app_loyer_edit', {'id': loyer.id}) }}">edit</a>

    {{ include('loyer/_delete_form.html.twig') }}
{% endblock %}
