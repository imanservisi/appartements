<form method="post" action="{{ path('app_locataire_delete', {'id': locataire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ locataire.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
