<form method="post" action="{{ path('app_regularisation_ponctuelle_delete', {'id': regularisation_ponctuelle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ regularisation_ponctuelle.id) }}">
    <button class="btn">Delete</button>
</form>
