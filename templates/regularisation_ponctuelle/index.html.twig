{% extends 'base.html.twig' %}

{% block title %}RegularisationPonctuelle index{% endblock %}

{% block body %}
    <h1>RegularisationPonctuelle index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Annee</th>
                <th>Montant</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for regularisation_ponctuelle in regularisation_ponctuelles %}
            <tr>
                <td>{{ regularisation_ponctuelle.id }}</td>
                <td>{{ regularisation_ponctuelle.annee }}</td>
                <td>{{ regularisation_ponctuelle.montant }}</td>
                <td>
                    <a href="{{ path('app_regularisation_ponctuelle_show', {'id': regularisation_ponctuelle.id}) }}">show</a>
                    <a href="{{ path('app_regularisation_ponctuelle_edit', {'id': regularisation_ponctuelle.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_regularisation_ponctuelle_new') }}">Create new</a>
{% endblock %}
