{% extends 'base.html.twig' %}

{% block title %}Mandat Syndic{% endblock %}

{% block body %}
    <h1 class="text-center">Modifier Mandat Syndic</h1>

    {{ include('mandat_syndic/_form.html.twig', {'button_label': 'Modifier'}) }}

    <button class="mt-3 btn btn-secondary">
        <a href="{{ path('app_residence_show', {id: residence.id}) }}">Retour résidence</a>
    </button>

    <div class="mt-3">
        {{ include('mandat_syndic/_delete_form.html.twig', {residenceId: residence.id, id: mandat_syndic.id}) }}
    </div>
{% endblock %}
