<form method="post" action="{{ path('app_lot_delete', {'residenceId': residence.id, 'id': lot.id}) }}" onsubmit="return confirm('Etes vous sûr de vouloir supprimer ce lot ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lot.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
