<form method="post" action="{{ path('app_travaux_delete', {'residenceId': residence.id, 'lotId': lot.id, 'id': travaux.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ travaux.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
