{% extends 'base.html.twig' %}

{% block title %}Travaux{% endblock %}

{% block body %}
    <h1>Travaux</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ travaux.id }}</td>
            </tr>
            <tr>
                <th>DateTravaux</th>
                <td>{{ travaux.dateTravaux ? travaux.dateTravaux|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>TypeTravaux</th>
                <td>{{ travaux.typeTravaux }}</td>
            </tr>
            <tr>
                <th>MontantTravaux</th>
                <td>{{ travaux.montantTravaux }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_travaux_index') }}">back to list</a>

    <a href="{{ path('app_travaux_edit', {'id': travaux.id}) }}">edit</a>

    {{ include('travaux/_delete_form.html.twig') }}
{% endblock %}
